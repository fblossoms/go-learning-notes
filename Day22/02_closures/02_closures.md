# 闭包
- 自由变量
  - 不在本地作用域中定义的局部变量（不是全局变量）。例如定义在内层函数外的外层函数的作用域的局部变量
- 闭包
  - 出现在嵌套函数中，指的是内层函数引用了外层函数的自由变量，就形成了闭包
  - 闭包是一个动态的概念
  - 何时考虑
    - 函数有嵌套，函数内定义了其他函数
    - 内部函数使用了外部函数的局部变量
    - 内部函数被返回（非必须）
- 变量逃逸
  - 把存放数据的地方改到堆heap上（从栈逃逸到堆上），只有闭包时才会考虑变量逃逸