# 结构体指针
- 结构体是非引用类型，使用的是值拷贝
- new内建函数
  - new(类型)
    - 构建该类型的实例，返回该实例的指针

```txt
            return &类型{}

```

- 成员如何访问
  - 实例.成员

```txt
            var u1 user
```

    - u1.name
    - (&u1).name

```txt
            p1 := &u1
```

    - (*p1).name